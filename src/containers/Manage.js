import React from 'react'
import BigText from '../components/BigText';

const Manage = () => {
    return(
        <div>
            <BigText>관리자모드</BigText>
        </div>
    );
};

export default Manage;